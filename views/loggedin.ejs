<!DOCTYPE html>
<html class="h-100" lang="en">
  <head>
    <title>
      <%= siteTitle %>
    </title>
    <!-- Basic Meta -->
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="shortcut icon" href="/favico.ico" type="image/x-icon" />
    <!-- Custom -->
    <link rel="stylesheet" type="text/css" href="/assets/css/main.css" />

    <!-- Site meta data -->
    <meta name="description" content="<%= siteDesc %>" />
  </head>

  <body class="flex items-center h-full py-12 mb-0 bg-priamry-400">
    <div class="w-1/2 p-6 m-auto bg-gray-400 rounded-md shadow-md">
      <div class="mb-4 text-center">
        <h1 class="text-2xl font-medium leading-10 align-middle">
          <%= siteTitle %>
        </h1>
      </div>
      Welcome <strong> <%= currentUser.username %></strong> your API key is
      below
      <form action="/key" method="POST" novalidate>
        <input type="hidden" name="_csrf" value="<%= _csrf  %>" />
        <div class="mb-3 input-group">
          <input
            type="text"
            class="form-control"
            placeholder="Your API Key"
            aria-label="Your API Key"
            aria-describedby="basic-addon2"
            value="<%= currentUser.apiKey %>"
            disabled
          />
          <div class="input-group-append">
            <button class="btn btn-outline-primary" type="submit">
              <% if(!currentUser.apiKey) {%> Get Key <%} else { %> Reset Key <%
              } %>
            </button>
          </div>
        </div>
      </form>
      <a
        href="/logout"
        class="block w-full p-2 my-3 text-xl font-medium text-center text-white no-underline bg-green-500 border-8 border-transparent hover:text-white hover:bg-green-400"
      >
        Logout
      </a>
    </div>
  </body>
</html>
